/**
 * Swarm coordination layer.
 * STATUS: stub — no swarm logic implemented.
 */
import { registerSubsystem } from '@sim/core/status-labels';
registerSubsystem('swarm', 'stub', 'No swarm coordination');
